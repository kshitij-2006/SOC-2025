You can run the UI on hugging face spaces, the Dockerfile is contains command that structure the workspace.

Sometimes the compilation of dlib fails and in that case try to directly download the compiled binaries.